<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.control4.yamba"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="16" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:name=".YambaApp" >
        <activity
            android:name="com.control4.yamba.StatusActivity"
            android:label="@string/title_activity_status" >
        </activity>
        <activity android:name="com.control4.yamba.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".RefreshService" >
        </service>

        <provider
            android:name=".StatusProvider"
            android:authorities="com.control4.android.yamba.provider.timeline" >
        </provider>

        <receiver android:name=".BootReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        
        <receiver android:name=".NotificationReceiver">
            <intent-filter >
                <action android:name="com.control4.yamba.action.NEW_STATUS"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>